# Postgres. The app talks to it through @prisma/adapter-pg.
DATABASE_URL="postgresql://USER@localhost:5432/ascendblog?schema=public"

# Where uploaded images are written. Absolute path in production so it can be a
# mounted volume that survives deploys. Defaults to ./var/uploads.
# UPLOADS_DIR="/var/lib/ascendblog/uploads"
